function setup() {
  createCanvas(400, 400);
}
function draw() {
  if(mouseIsPressed && mouseX>150 && mouseY>150) {
    background (0,500,0);
  } else background (250,250,250);{
  }
  rect(150,150,100,100);

}
